import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

const coversationSlice = createSlice({
    name: 'conversation',
    initialState,
    reducers: {

    }
})


export const {} = coversationSlice.actions;
export default coversationSlice.reducer;